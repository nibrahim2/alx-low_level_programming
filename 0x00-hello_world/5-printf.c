#include <stdio.h>
/**
 * main -entry point to the main block
 * Return: returns 0 at the end of the compiled program
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
