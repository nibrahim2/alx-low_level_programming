#include "main.h"

/**
 * mul - checks for a digit from 0 through 9
 * @a: a - variable
 * @b: b - variable
 * Return: returns 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
