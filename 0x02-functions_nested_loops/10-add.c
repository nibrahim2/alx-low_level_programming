#include "main.h"
/**
 * add -sums two integers
 * @x: the first integer to be computed
 * @y: the second integer to be computed
 * Return: returns the result of summation
 */
int add(int x, int y)
{
	return (x + y);
}
